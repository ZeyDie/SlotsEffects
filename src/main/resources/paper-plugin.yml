name: ${name}
version: ${version}
main: com.zeydie.slotseffect.mountcore.SlotsEffect
api-version: '1.21'
# Usage Paper Plugin Loader API
#loader: ru.mountcode.plugins.example.ExampleDependencyLoader
prefix: ${name}
authors: [ ${authors} ]
description: ${description}
website: ${website}
dependencies:
  server:
    MountCore:
      required: true
      join-classpath: true
      load: BEFORE